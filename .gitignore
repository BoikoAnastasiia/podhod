node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.*
!.env.example
*.local

# Raw dataset download — 17 MB, rebuilt by `pnpm data:fetch`.
# The stripped seed artifacts under data/ are committed on purpose.
data/.cache/

# Regenerated from the committed data artifacts by `pnpm seed:emit`.
apps/api/seed.sql

coverage/
playwright-report/
test-results/
# Scratch output from browser-automation tooling.
.playwright-mcp/

.DS_Store
*.log

# Personal scratch notes, not part of the project.
NEXT-STEPS.md
